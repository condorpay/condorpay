---
"@condorpay/sdk": minor
---

Publish the unified SDK under the scoped name `@condorpay/sdk` (subpath `@condorpay/sdk/co`) so all npm packages use the `@condorpay` scope and granular CI tokens apply.
